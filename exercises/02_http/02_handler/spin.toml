spin_manifest_version = 2

[application]
name = "handler"
version = "1.0.0"

[[trigger.http]]
route = "/..."
component = "handler"

[component.handler]
source = "../../../target/wasm32-wasip1/debug/handler.wasm"

[component.handler.build]
command = "cargo build -p handler --target wasm32-wasip1"
watch = ["src/**/*.rs", "Cargo.toml"]

[component.handler.tool.spin-test]
source = "../../../target/wasm32-wasip1/debug/handler_tests.wasm"
